name: Directional ADI9 survival gate

on:
  push:
    branches: [master]
    paths:
      - 'scripts/directional_adi9_survival_gate.py'
      - '.github/workflows/directional-adi9-gate.yml'
  workflow_dispatch:

jobs:
  prove-and-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/directional_adi9_survival_gate.py
      - uses: actions/upload-artifact@v4
        with:
          name: directional-adi9-survival-gate
          path: results/directional_adi9_survival_gate.json
          retention-days: 90
