name: MPRC Vision CIFAR v0

on:
  push:
    branches: [master]
    paths:
      - 'scripts/mprc_vision_cifar_v0.py'
      - '.github/workflows/mprc-vision-cifar-v0.yml'
  workflow_dispatch:

jobs:
  train-classify:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install numpy
      - run: python scripts/mprc_vision_cifar_v0.py
      - uses: actions/upload-artifact@v4
        with:
          name: mprc-vision-cifar-v0-result
          path: results/mprc_vision_cifar_v0.json
          retention-days: 90
